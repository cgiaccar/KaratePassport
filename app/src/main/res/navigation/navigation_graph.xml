<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_graph"
    app:startDestination="@id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="it.units.karatepassport.HomeFragment"
        android:label="Home"
        tools:layout="@layout/home_content" />
    <fragment
        android:id="@+id/nav_belt_log"
        android:name="it.units.karatepassport.BeltLog"
        android:label="Your Belts"
        tools:layout="@layout/activity_belt_log" />
    <fragment
        android:id="@+id/nav_grant_belt"
        android:name="it.units.karatepassport.GrantBelt"
        android:label="Grant a Belt"
        tools:layout="@layout/activity_grant_belt" />

    <activity
        android:id="@+id/nav_logout"
        android:name="it.units.karatepassport.Login"
        android:label="activity_login"
        tools:layout="@layout/activity_login" />

</navigation>